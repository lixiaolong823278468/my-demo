# 日前电价预测 — 技术文献调研与改进建议

> 基于 2024-2025 年国际与中国电力现货市场预测领域的最新研究，结合当前项目现状的对比分析。

---

## 一、领域研究现状概览

### 1.1 XGBoost 仍是日前电价预测的基准模型

2025 年多项独立研究在土耳其、美国 CAISO、希腊/比利时/爱尔兰等市场上横向对比了 XGBoost、LightGBM、CatBoost、LSTM、SVM、RF 等模型，结论高度一致：

- **XGBoost 在精度-速度权衡上最优**。土耳其市场（2025，ScienceDirect）：XGBoost 的 R² 达到 0.923，MAE=144.8 TRY/MWh，训练仅需 94 秒，显著优于 LSTM 和 SVM。
- **LightGBM 在部分数据集上略优**。欧盟市场研究（2025，arXiv）：LightGBM 在价格尖峰检测上表现最好，XGBoost 紧随其后。建议同时训练两者做对比。
- **45-60 天的训练窗口**在精度和时效性之间取得最佳平衡——过长的历史数据反而引入分布偏移。

你当前项目用 XGBoost 作为核心模型，方向完全正确。

### 1.2 中国电力现货市场的独特挑战

山东现货市场研究（2024，Applied Energy，华能清洁能源研究院）指出中国市场的特殊性：

- **新能源高比例渗透**（风光）导致净负荷波动剧烈，电价序列非平稳性远超欧美市场
- **相似日算法（DSA）**在中国市场被证明非常有效——从历史相似日中提取电价特征
- **模态分解（CEEMDAN/MVMD）**在中文文献中几乎成为标配，用于将非平稳电价序列拆解为平稳分量
- 山东 96 点预测达到 MAE=0.138（归一化后），该论文用 XGBoost 做特征选择 + DNN 做预测

你项目的"火电空间 + 多条件相似法"思路和 DSA 高度吻合，已经走在对的方向上。

### 1.3 2025 年最热方向：概率预测与 Conformal Prediction

如果说 2024 年的关键词是"Transformer"，那 2025 年的关键词毫无疑问是 **Conformal Prediction（共形预测）**：

| 研究方向 | 代表性工作 | 核心贡献 |
|----------|-----------|----------|
| CP + 日前市场 | O'Connor et al., Energy and AI (2025) | 提出 Q-Ens：Quantile Regression + CP Ensemble，预测区间更窄的同时保持覆盖率 |
| 在线 CP | Applied Energy (2025) | CQR + 对抗共形推理，处理时间序列的非交换性 |
| iQRA | arXiv (2025) | 保序分位数回归平均，声称在可靠性上优于覆盖率基 CP |
| CP + 电池套利 | Energy and AI (2025) | 首次用真实交易模拟验证概率预测的经济价值 |

**核心结论**：只给一个点预测值已经不够了。2025 年的最佳实践是给出 **预测区间（prediction interval）**，让使用者知道"这个预测有多不确定"。这对交易决策至关重要。

### 1.4 特征工程的最新共识

CAISO 市场研究（2025，MDPI Forecasting）用 SHAP 分析了特征的贡献度：

| 特征类型 | 贡献度 | 说明 |
|----------|--------|------|
| `lag_1h` | ~25% | 前一小时价格，最强的单特征 |
| `lag_24h` | ~18% | 前一天同时段价格（你的 lag_96 等价于这个） |
| 移动平均 (MA_3h ~ MA_30d) | ~15% | 多尺度平滑捕捉趋势 |
| 循环时间编码 (sin/cos) | ~10% | 保留日/周/月周期性 |
| 负荷预测 | ~12% | 供需基本面 |

你项目的 `lag_96`（前一天同时段）正是排名第二的强特征，但缺少了 `lag_1h`（前一时段）和多尺度移动平均。

---

## 二、你的项目 vs. 学术界最佳实践：差距分析

### 2.1 做得好的部分（保持）

| 设计决策 | 学术支撑 |
|----------|----------|
| **XGBoost 为核心模型** | 2024-2025 多项研究验证为最优基准 |
| **5 段时间片分割** | 不同时段的定价机制确实不同（凌晨边际成本定价 vs 晚高峰供需紧张定价） |
| **多条件相似日加权**（火电空间、开机容量、日类型） | 与华能论文的 DSA 算法思路一致，中国市场特别有效 |
| **lag_96（前一天价格）** | SHAP 分析显示贡献 ~18%，是最重要的特征之一 |
| **数据质量校验管线** | 工程实践优秀，很多论文忽略了这一步 |
| **模型版本管理与回滚** | MLOps 意识好 |

### 2.2 需要改进的部分

按优先级从高到低排列：

**P0 — 必须做**

1. **当前模型是旧版，新代码未生效**：`models/` 目录下的模型是 legacy 格式（9 特征，80 轮训练，平铺结构），而代码已经支持双变体直接预测（12 特征，400 轮，`model_variants` 结构）。需要重新训练一次让新版生效。

2. **预测方式不一致**：旧版用 `price = similar_price + model_residual`（残差预测），新版代码用 `model_pred` 直接预测价格。两种方式在 `run_prediction_with_strategy` 中由 `metadata_has_direct_variants` 判断分支。当前实际走的是旧版残差路径。

**P1 — 高优先级**

3. **缺少概率预测**：当前只输出点预测值。建议至少加上分位数回归（Quantile Regression），给出 P10/P50/P90 预测区间。XGBoost 原生支持 `objective=reg:quantileerror`，改动成本很低。

4. **特征集偏窄**：当前 9 个特征中缺少：
   - 循环时间编码（`hour_sin/cos`）— 保留 24 小时周期性
   - 短期滞后（`lag_1h` ~ `lag_4h`）— 前几个时段的实际价格
   - 移动平均（`MA_4h`, `MA_24h`, `MA_7d`）— 多尺度趋势
   - 价格波动率（`rolling_std_24h`）— 市场紧张程度

5. **训练轮数不足**：当前 `num_boost_round=80`（legacy 模型），代码默认是 400，但文献中使用 Optuna 等自动调参在 300-1000 轮之间搜索最优值。

**P2 — 中优先级**

6. **缺少模型集成**：文献反复验证 ensemble（stacking/voting）优于单模型。建议至少加一个 LightGBM 作为第二模型，简单平均就能提升稳健性。

7. **`similar_price` / `similar_gap` 未入模**：当前 active 模型的 9 个特征中不包含这两个相似法特征，而新版代码的 12 个特征包含了。相似价格作为特征输入 XGBoost 可以让模型学会"何时信任相似法、何时偏离"。

8. **无模型可解释性**：SHAP 分析已成为 2024-2025 论文的标配。建议训练后跑一次 SHAP，了解各特征的实际贡献。

**P3 — 低优先级（锦上添花）**

9. **模态分解**：CEEMDAN/VMD 将原始电价序列分解为多个 IMF 分量再分别预测。中文论文中效果显著，但工程复杂度高，且可能引入 look-ahead bias。

10. **深度学习**：LSTM/Transformer 在中文文献中广泛应用，但在欧美市场的 head-to-head 对比中并未显著优于 XGBoost。除非数据量极大（>5 年日频），否则边际收益有限。

---

## 三、具体改进路线图

### 第一阶段：让现有代码发挥全部能力（1-2 天）

```
1. 重新训练模型（train_and_register）
   - num_boost_round 设为 400（或更大）
   - 确认产出双变体（with_lag_96 / no_lag_96）
   - 确认 metadata 包含 model_variants 字段

2. 验证预测流程
   - 确认 prediction_method 走 direct_price_xgboost 分支
   - 对比新旧预测结果差异
```

### 第二阶段：特征工程增强（3-5 天）

```
在 FEATURE_COLUMNS 中新增：
  - hour_sin, hour_cos        （循环时间编码）
  - lag_1period, lag_4period   （短期滞后价格）
  - MA_16period, MA_96period   （移动平均）
  - rolling_std_24period       （价格波动率）
  - similar_price, similar_gap （已在代码中，确认生效）

这些特征的构造都不需要额外数据源，完全可以从现有历史数据中派生。
```

### 第三阶段：概率预测（3-5 天）

```
方案 A（低成本）：XGBoost 分位数回归
  - 分别训练 q=0.1, 0.5, 0.9 三个模型
  - 输出 P10/P50/P90 预测区间
  - 改动：约 20 行代码

方案 B（中等成本）：Conformal Prediction
  - 在现有点预测基础上加 CP 后处理
  - 用验证集的残差分布构造自适应预测区间
  - 改动：约 50 行代码，但效果更好
```

### 第四阶段：模型集成（3-5 天）

```
- 加入 LightGBM 作为第二模型
- 简单平均集成（voting）即可
- 可选：训练一个简单的线性 stacking 元模型
```

---

## 四、关键技术细节参考

### 4.1 循环时间编码（Cyclical Encoding）

```python
# 替代当前的 hour (0-23 整数)
df['hour_sin'] = np.sin(2 * np.pi * df['hour'] / 24)
df['hour_cos'] = np.cos(2 * np.pi * df['hour'] / 24)

# 同理 weekday (0-6) 和 month (1-12)
df['weekday_sin'] = np.sin(2 * np.pi * df['weekday'] / 7)
df['weekday_cos'] = np.cos(2 * np.pi * df['weekday'] / 7)
```

这样做的好处：让模型知道 23 点和 0 点是相邻的（整数编码会丢失这个信息）。

### 4.2 XGBoost 分位数回归

```python
# 现有：objective='reg:squarederror' → 预测均值
# 改为分位数：
params_q10 = {**DEFAULT_XGB_PARAMS, 'objective': 'reg:quantileerror', 'quantile_alpha': 0.1}
params_q50 = {**DEFAULT_XGB_PARAMS, 'objective': 'reg:quantileerror', 'quantile_alpha': 0.5}
params_q90 = {**DEFAULT_XGB_PARAMS, 'objective': 'reg:quantileerror', 'quantile_alpha': 0.9}
```

三个模型分别预测 P10（悲观）、P50（中性）、P90（乐观），输出区间 [P10, P90]。

### 4.3 简单 Conformal Prediction

```python
# 在验证集上计算残差
residuals = np.abs(y_valid - y_pred_valid)

# 取 (1-alpha) 分位数作为区间半径
alpha = 0.1  # 90% 置信
radius = np.quantile(residuals, 1 - alpha)

# 预测时
lower = y_pred - radius
upper = y_pred + radius
```

这是最基本的 CP 实现，EnbPI 和 SPCI 等进阶方法可处理时间序列的非平稳性。

---

## 五、参考文献

1. Huang et al. (2024). "A hybrid framework for day-ahead electricity spot-price forecasting: A case study in China." *Applied Energy*, 373, 123863. — 山东现货市场，XGBoost 特征选择 + DNN，与你项目最相关。
2. Ibebuchi (2025). "Day-Ahead Energy Price Forecasting with Machine Learning: Role of Endogenous Predictors." *Forecasting*, 7(2), 18. — CAISO 市场，SHAP 特征分析，lag_1h 贡献 ~25%。
3. 马强等 (2025). "基于电价形成机制与 XGBoost 的统一电力现货市场价格预测." *系统科学与数学*, 45(5). — 中国统一市场，Optuna 超参优化。
4. O'Connor et al. (2025). "Conformal Prediction for electricity price forecasting in the day-ahead and real-time balancing market." *Energy and AI*, 21, 100571. — Q-Ens 集成 + CP，附带电池套利验证。
5. Lipiecki & Uniejewski (2025). "Isotonic Quantile Regression Averaging for uncertainty quantification." *arXiv:2507.15079*. — iQRA，德国市场，优于覆盖率基 CP。
6. 土耳其 XGBoost 研究 (2025). "Day-Ahead Electricity Price Forecasting Using XGBoost." *ScienceDirect*. — XGBoost vs LSTM/SVM/RF，R²=0.923。
7. 吴其东 (2025). "基于改进神经网络的电力现货市场日前价格预测方法." *电气技术与经济*(10). — CEEMDAN 模态分解 + 神经网络。

---

> **总结**：你的项目基础扎实——XGBoost + 分时段 + 相似法的组合在学术上是被充分验证的正确路线。当前最大的两个卡点是 (1) 新版代码未生效，(2) 缺少概率预测能力。优先解决这两个，再逐步补齐特征工程和模型集成，就能对齐 2025 年的学术最佳实践。
